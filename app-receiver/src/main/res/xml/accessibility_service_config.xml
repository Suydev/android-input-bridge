<?xml version="1.0" encoding="utf-8"?>
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeAllMask"
    android:accessibilityFlags="flagDefault|flagReportViewIds"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:canPerformGestures="true"
    android:canRequestFilterKeyEvents="true"
    android:canRetrieveWindowContent="true"
    android:description="@string/accessibility_service_description"
    android:notificationTimeout="100" />
<!--
    BUG-031 FIX: Added android:canRetrieveWindowContent="true"

    Without this flag, rootInActiveWindow returns null for most apps. Every call to
    injectKeyCode() and injectText() begins with:
        val root = rootInActiveWindow
        if (root == null) { ... return }
    meaning ALL keyboard injection, text injection, Ctrl shortcuts, and arrow-key movement
    were silently blocked for every normal app. Only accessibility navigation (BACK, HOME,
    RECENTS) and gesture injection (tap, swipe) worked without this flag.

    With canRetrieveWindowContent="true", rootInActiveWindow returns the actual window tree
    for any foreground app, enabling getFocused() to find the focused text node and all
    ACTION_SET_TEXT / ACTION_PASTE injection to work correctly.
-->
